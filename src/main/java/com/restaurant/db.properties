# H2 嵌入式数据库（~ 在 Windows 上指向 %USERPROFILE%，在 Linux/Docker 指向 /root/）
db.url=jdbc:h2:file:~/restaurant_db;MODE=MySQL;DB_CLOSE_DELAY=-1
db.username=sa
db.password=
db.driver=org.h2.Driver
db.pool.size=10
